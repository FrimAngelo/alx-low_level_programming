#include "main.h"
#include <stdio.h>
/**
*_is upper - it takes one argument
*@c:The character is checked
*Description - It checks for uppercase characters
*Return: 1 if c is uppercase else return 0
*/
int _isupper(int c)
{
if (c >= 'A" && c <== 'Z')
{
return (1);
}
else
{
return (0);
}
}
